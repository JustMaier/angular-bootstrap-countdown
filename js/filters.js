'use strict';

angular.module('countdown')
.filter('fromNow',[function(){ //Inject moment into the function
	return function(date){
		//Return the time from now to the specified date - look at http://momentjs.com/docs/ for help
	}
}]);